# Load report — 2026-08-10 — smoke

> **LOCAL DEVELOPMENT RUN — not publishable evidence.** Loopback timings
> exclude the network path and shared-platform contention; the repository
> evidence discipline forbids publishing them as deployed numbers.

| | |
| --- | --- |
| Profile | smoke |
| Target host | 127.0.0.1 |
| Image | `5080f4f0758c` |
| Environment | sandbox loopback (harness development) |
| Generated | 2026-08-10T21:20:52.552Z |
| Total requests | 3067 |
| 5xx responses | 0 |
| 429 responses | 0 (rate limiter working as designed; open-original is per-IP by design and excluded from throughput scenarios) |

## Stated assumptions

300 req/s sustained ≈ ~35k DAU at 10% peak concurrency × 5 req/min per active
user. The 600 req/s burst models a viral read spike. A 3,000 req/s ramp is a
10× headroom probe, not a DAU claim. If Node saturates before Postgres, the
recorded rate is the documented trigger condition for the Rust API seam in
docs/PERFORMANCE.md — "measured the Node ceiling at X" is the claim that
matters.

## Latency by endpoint

| Endpoint | Scenario | p50 | p95 | p99 |
| --- | --- | --- | --- | --- |
| feed | pass | 37.9ms | 45.4ms | 58.1ms |
| permalink | pass | 7.3ms | 10.5ms | 12.8ms |
| oembed | pass | 5.8ms | 8.8ms | 10.9ms |
| search | pass | 81.5ms | 93.5ms | 107.2ms |
| like | pass | 21.6ms | 26.3ms | 38.5ms |
| publish | pass | 12.0ms | 13.5ms | 13.6ms |

## Thresholds applied

- `http_req_duration{name:feed,scenario:pass}`: p(95)<500
- `http_req_duration{name:like,scenario:pass}`: p(95)<300
- `http_req_duration{name:oembed,scenario:pass}`: p(95)<500
- `http_req_duration{name:permalink,scenario:pass}`: p(95)<500
- `http_req_duration{name:publish,scenario:pass}`: p(95)<300
- `http_req_duration{name:search,scenario:pass}`: p(95)<500
- `server_errors_5xx`: count<=0

## Media drain

| Measure | Value |
| --- | --- |
| Jobs | 10 |
| Workers | 2 (one killed mid-drain) |
| Ready | 10/10 |
| Clips/min per worker | 4.79 |
| Queue depth where time-to-ready exceeds 60s | 4 |
| Double claims | 0 |
| Lease recoveries | 2 |
| Terminal states | ready: 10 |


---
Generated by `load/report.mjs` from `2026-08-10-smoke.summary.json` and `2026-08-10-drain.json`. Budgets live in docs/PERFORMANCE.md; change them there first.
