# Annotated brief acceptance

Last verified: 2026-08-04 against <https://annotated.lovable.app/>. The
handoff URL <https://annotated.com/brief> returned HTTP 404 on that date; the
reachable Lovable brief is the current source used here and describes the same
Chrome-sidebar, source-link, claim, identity, media, commentary, and social-feed
requirements recorded below.

Status meanings: `done` has current code and direct validation evidence; `partial` has a working slice but misses part of the brief or production boundary; `missing` has no working implementation; `blocked` requires owner credentials or external access for final proof.

| Requirement | Status | Current evidence | Completion evidence still required |
| --- | --- | --- | --- |
| Chrome side-panel extension is the primary capture surface | partial | Manifest V3 side panel reads the active tab and selected text, reloads on tab activation/navigation, uses an HTTPS-required deployed API origin, expires session tokens, exchanges OAuth tickets for bearer sessions, records bounded audio in IndexedDB, uploads server-side media, and queues retryable publishes in bounded local metadata; bounded client request IDs deduplicate local retries, uploaded audio IDs survive annotation retries, 401 responses clear the stale session and preserve the capture as `needs-auth`, and repeated/non-retryable failures remain visible as bounded blocked items. The extension now declares Chrome 114+, handles service-worker setup/open/retry failures, uses a session-backed retry lock and startup/manual/alarm recovery, validates stored API origins on read, ships real 16/48/128 PNG icons from an authored source, has a runtime-only packaging script plus a Chrome Web Store readiness record, and uses an explicit `[hidden]` state contract, inline SVG icon language, accessible pressed/status state, and reduced-motion handling. The API-origin settings page now gives a branded, keyboard-friendly connection form with explicit HTTPS/local guidance, a local-default recovery action, and visible save/error status. The deployed staging origin now serves the same v0.1.0 build with PostgreSQL-backed readiness, a ready media runtime, the privacy policy, and the canonical brand asset; `npm run acceptance:staging` makes those public checks repeatable. | Docked packaged-browser acceptance, microphone/offline/service-worker recovery, screenshots/promo art, and publisher contact details. |
| Clip media, text, audio, or video from a website | partial | Web capture handles all three source categories; extension captures selected text and media ranges. | End-to-end provider clips from the extension for YouTube and podcasts. |
| Text and recorded-audio commentary | partial | Web capture and the extension record, stage, upload, publish, and replay audio; extension audio drafts stay in IndexedDB and queued metadata contains no Blob. Audio uploads now enforce an allowlisted MIME contract and a per-actor process-local rate limit with `429`/`Retry-After` responses. | Packaged extension audio browser evidence and production media delivery. |
| Public annotation landing page | partial | Stable `/a/:slug` route reloads published state from the API. | Durable production persistence and clean-checkout browser evidence. |
| Every annotation links to its original source | partial | API preserves `sourceUrl` and `canonicalUrl`; `test/api-acceptance.test.js` now checks publish, public detail, and filtered-feed citations for article, video, and podcast annotations, and public pages render the source citation. | Deployed browser evidence that each public surface exposes and follows the source link. |
| Public social feed | partial | Published annotations, public `/u/:handle` profiles with bounded annotation lists, like counts, comments, filters, bounded server-backed search, cursor pagination, and native video/audio controls for ready media load from the server store. Public author payloads expose only display identity fields. Feed share controls now copy the clicked annotation's public URL, public annotation bylines link to the author's profile, and an empty server feed no longer renders synthetic social posts or hard-coded follower counts. `test/api-acceptance.test.js` now verifies follow/unfollow profile counts, the following-feed filter, comment persistence, and idempotent like/unlike counts. | Durable production database, authorization, and deployed evidence. |
| Follow other users | partial | Follow/unfollow endpoints persist a social graph and profile responses expose follower/following counts. | Multi-user OAuth browser evidence, abuse controls, and production database run. |
| Comment on annotations | partial | Comments persist with author metadata and the UI reloads them from the API. | Identity, authorization, abuse controls, and durable production tests. |
| Sign up with X or Google OAuth only | partial | Google is the first explicitly enabled production provider (`OAUTH_PROVIDERS=google`); X remains a provider-neutral sibling adapter enabled with `OAUTH_PROVIDERS=google,x`. Provider-scoped credentials, labels, profile normalization, PKCE starts/callbacks, exact return origins, malformed-cookie tolerance, provider timeouts, sessions, bearer/cookie logout, expiry, owner IDs, and one-time extension tickets are server-backed and tested. The web app now preserves the requesting page through OAuth, reports success/error/cancelled states, and prompts for an enabled provider before auth-required publish, like, follow, comment, and claim actions; API errors retain their HTTP status for session recovery. Local mode retains the development user. | Real provider credentials and callback success/cancellation/expiry browser evidence, plus deployed callback DNS/configuration and docked browser proof. |
| Paste a URL or use the active page | partial | Web app resolves pasted URLs; extension reads the active tab. | Unified production API configuration and browser coverage. |
| Support YouTube videos | partial | Metadata uses YouTube oEmbed; worker validates source URLs and invokes the configured `yt-dlp` adapter with a 240p-safe format. Production `/api/ready` now probes `YTDLP_BIN` alongside FFmpeg/FFprobe and refuses readiness when the provider runtime is unavailable. The production image pins and SHA-256 verifies the official 2026.06.09 standalone extractor for amd64/arm64, and the worker now has an explicit, validated `YTDLP_JS_RUNTIME`, `YTDLP_PROXY`, secret-mounted `YTDLP_COOKIES_FILE`, and optional `YTDLP_PLAYER_CLIENT` boundary. A real public YouTube fixture resolved, downloaded, and transcoded locally with FFprobe-confirmed 240p/audio output; the deployed Railway API also resolved `jNQXAC9IVRw` to `ready-for-range` with `provider: youtube`. | Deployed PostgreSQL/S3 worker run, successful provider configuration/fixture, failure/retry evidence, and production browser playback. |
| Support news articles with selected passage and metadata | partial | Resolver bounds response size, follows at most three redirects with URL and DNS-answer SSRF checks at every hop, extracts entity-decoded metadata/excerpts, preserves canonical URLs, and the web capture desk now exposes an editable, persisted 2,000-character selected-passage field. The extension blocks article publishing until a page selection exists, and the API requires/bounds `sourceExcerpt` for article annotations. The deployed Railway API returned `text-ready` metadata for `https://example.com/`; selected-passage browser evidence remains external. | Robust extraction across real article fixtures and selected-passage browser evidence. |
| Support podcasts | partial | Resolver recognizes direct audio, podcast-host URLs, and RSS/Atom `feed`/`rss`/`atom` URLs; bounded feed parsing extracts the first episode title, author, description, artwork, canonical show link, and safe enclosure URL, and the worker now hands extensionless signed enclosures directly to FFmpeg after public-URL validation before falling back to the configured `yt-dlp` audio adapter. | Provider extraction, retry/recovery, and browser playback evidence. |
| Choose media start/end or exact text passage | partial | Web/extension range controls enforce bounds; active-tab text selection is captured. | Extension provider playback alignment and browser evidence. |
| Generate clip and public landing page | partial | Publishing creates the page immediately and queues FFmpeg processing; ready public/feed annotations render native video/audio controls, while queued and failed states remain explicit. Jobs now have bounded retries, recovery, owner cancellation, provider URL revalidation, object cleanup on abandoned output, FFprobe inspection before readiness, and killable provider/FFmpeg/FFprobe execution deadlines that feed the existing retry state. Failed public annotations now show the bounded worker error and an owner-only `Retry clip` action that supersedes the failed job before queueing a fresh attempt. The acceptance suite now generates a real local video fixture, runs the production FFmpeg arguments, and inspects the resulting artifact with FFprobe before accepting it. A guarded Railway staging smoke passed through the production worker, private media redirect, signed object delivery, and cleanup for a direct-audio podcast fixture. | Authenticated OAuth publishing, deployed YouTube extraction, and browser evidence. |
| Maximum 90-second video/audio clips | partial | API validation rejects longer ranges and worker clamps duration. The generated-fixture acceptance test inspects the actual output duration and enforces the 90-second ceiling; the guarded staging worker smoke runs a one-second direct-audio fixture through the production boundary. | Deployed video-worker output-duration evidence and browser playback. |
| Video output is 240p and below 480p | partial | Worker passes `scale=-2:240` to FFmpeg and rejects inspected output above 240px high. The generated-fixture acceptance test now produces a real MP4 and verifies its FFprobe height is `240` and width is no more than `480`, with an audio stream. | Deployed worker fixture and browser playback evidence. |
| Clearly visible `File a claim` on every annotation | partial | Public-page claim card submits a persisted claim; feed and profile annotation cards now expose a visible `File a claim` action that opens the same slug-scoped form; active reports are deduplicated, reporters can query `/api/claims`, and the owner/moderator web queue exposes source context, reporter, restricted status transitions, and audit-backed updates. Claim submission and moderator status mutations now use atomic PostgreSQL-backed per-actor rate-limit buckets in production, with a bounded in-process fallback for local development and `429`/`Retry-After` responses. | Authenticated production moderation, distributed edge controls, and durable storage/recovery evidence. |

## Production readiness gates

- [ ] PostgreSQL is the production metadata repository; migrations and integration tests cover annotations, users, comments, claims, follows, likes, and media jobs. PR33 runs the migration ledger and transactional repository against real PostgreSQL, and PR35’s hosted integration round-trips every persisted product collection through the entity-record table; `npm run backup:production` captures a custom-format dump, schema/collection snapshot, S3 object inventory, and bucket versioning/lifecycle state without mutating production, while `npm run backup:verify` checks the artifact and can run an explicitly isolated restore drill; scheduled snapshots, provider retention configuration, and a completed live recovery remain external.
- [ ] Published media uses configured S3/R2-compatible object storage and a delivery URL; local files remain development-only. PR33 now exercises a real S3-compatible bucket, signed URL, upload, and deletion in hosted CI; a durable production bucket/CDN remains external.
- [ ] Google and X OAuth, secure sessions, ownership, logout, expiry, and extension authentication are real and tested. PR37 executes the PKCE callback/session/one-time-extension-ticket protocol in an isolated provider fixture; PR49 makes Google the first enabled production provider and keeps X as an explicitly configured sibling; PR50 adds page-preserving web callbacks and visible auth recovery for protected mutations. Real provider credentials, consent/cancellation, callback DNS, and deployed browser evidence remain external.
- [ ] Extension API origin, offline queueing, retry states, and service-worker/side-panel recovery are production-configurable and browser-tested.
- [ ] Provider extraction and FFmpeg processing enforce duration/resolution, retries, cancellation/recovery, cleanup, and visible failures; production readiness probes the actual FFmpeg/FFprobe/provider runtime and the image pins the provider executable. PR36 adds persisted worker leases, duplicate-claim protection, and restart recovery for queued or expired processing jobs; the hosted image smoke now runs the generated-media/FFprobe contract inside both amd64 and arm64 production images; a managed queue and live deployment remain external.
- [ ] Feed, profiles, follows, likes, comments, sharing, pagination, and authorization persist server-side.
- [ ] Claims have review states, audit records, rate limits, and user-visible reporting outcomes. Production PostgreSQL now has a shared hashed-key rate-limit ledger for claims and other mutations; the Railway staging smoke proves atomic allow/deny behavior, while multi-instance and distributed edge evidence remain external.
- [ ] CORS, secure headers, request limits, URL and DNS-answer SSRF defenses, bounded request telemetry, health/readiness, reproducible non-root image builds, and migrations are documented and tested; hosted CI now passes amd64/arm64 image and provider-runtime checks plus a real container-network smoke with PostgreSQL and MinIO (`/api/ready` reports `ready`, PostgreSQL persistence, and a ready media runtime), while public deployment, durable-service backups, recovery, and live traffic remain external.
- [ ] A clean-checkout automated suite and Chrome browser acceptance run pass with evidence linked from the final draft PR.

## Dated staging evidence

- 2026-08-04: Railway deployment `35651b01-9801-4a04-b2b2-46d29a2d4d06` applied migration `004_rate_limit_buckets`; `STAGING_ORIGIN=https://annotated-staging.up.railway.app npm run acceptance:staging` passed, and the guarded shared smoke returned `shared: true`, `allowed: [true, true, false]`, and `retryAfter: 33` before deleting its temporary bucket. This proves the deployed single-instance PostgreSQL boundary, not multi-replica or edge WAF coverage.
- 2026-08-04: Railway staging deployment `3e0dddde-4edd-465d-b596-8977668f1789` at `https://annotated-staging.up.railway.app` returned `200` for `/api/health`, `/api/ready`, `/api/auth/providers`, `/`, and `/privacy.html`. Readiness reported PostgreSQL persistence plus `ffmpeg`, `ffprobe`, and `provider extractor`; the provider surface reported Google/X keys with X enabled; the feed was empty but server-backed; unauthenticated `/api/me` and `/api/claims` correctly returned `401`; the root HTML referenced `annotated-mark-32.png`.
- 2026-08-03: The same staging API resolved the public YouTube fixture `https://www.youtube.com/watch?v=jNQXAC9IVRw` as `sourceType: video`, `provider: youtube`, and `processing: ready-for-range`, and resolved `https://example.com/` as `sourceType: article` and `processing: text-ready`. This proves deployed source-resolution behavior, not a completed deployed worker transcode or browser playback run.
- 2026-08-03: `node --test test/media-policy.test.js` generated a 640×360 local fixture, transcoded a 1.5-second clip with the production worker arguments, and verified the real FFprobe report: measurable duration under 90 seconds, a 240px-high video stream, width no greater than 480px, and an audio stream. This closes the generated-artifact boundary locally; deployed worker output and playback remain external.
- 2026-08-04: PR65's hosted image run [30861513757](https://github.com/tcballard/annotated/actions/runs/30861513757) executed that fixture inside both shipped production images after the PostgreSQL/MinIO readiness smoke; amd64 and arm64 passed. This strengthens the boundary from host-runtime evidence to the pinned container runtime.
- 2026-08-04: The feed/profile render contract now exposes `File a claim` for every server-backed annotation card and routes the selected card's slug through the persisted claim API; `node --test test/claim-ui.test.js` provides a static regression check. Live browser interaction remains external.
- 2026-08-04: The claim-surface branch passed `STAGING_ORIGIN=https://annotated-staging.up.railway.app npm run acceptance:staging` against deployment `5a5fc2e3-1a25-44d9-8e94-9e469180f2c8`; health/readiness, Postgres persistence, public routes, privacy, anonymous claim boundaries, and X OAuth start/cancellation returned the expected statuses.
- 2026-08-04: The deployed Railway image's direct YouTube provider probe reached yt-dlp but returned YouTube HTTP 429/bot verification, so it is not counted as successful deployed YouTube extraction. This remains an explicit provider limitation rather than a fabricated pass.
- 2026-08-04: Bounded Railway probes using the pinned extractor's `web_creator`, `mweb`, `tv`, `android_vr`, `android`, and `web_embedded` clients all reached YouTube but received HTTP 429/sign-in bot verification. The new provider configuration boundary supports a managed `YTDLP_PROXY` and secret-mounted `YTDLP_COOKIES_FILE` without putting credentials in the image; no configured egress/cookie is present in staging, so YouTube remains unverified.
- 2026-08-04: The guarded `accept-staging-media.mjs` smoke was added for the Railway container. It requires an explicit staging flag, runs a direct-audio podcast fixture through the production worker and private S3 delivery path, and cleans the temporary database/object records before reporting success.
- 2026-08-04: The backup/recovery boundary now has a guarded `npm run backup:production` command. From a trusted runner with `pg_dump`, it requires production PostgreSQL/S3 configuration, writes a `0600` custom-format database dump plus a sorted object inventory and SHA-256 manifest, refuses local adapters and existing output files, and never mutates the live database or bucket. Provider-side versioning/retention and an isolated restore drill remain external gates.
- 2026-08-04: The backup evidence layer now records S3-compatible bucket versioning/lifecycle state and supports strict `BACKUP_REQUIRE_OBJECT_VERSIONING`/`BACKUP_REQUIRE_OBJECT_LIFECYCLE` gates. `npm run backup:verify` validates checksums, object totals, sorted keys, and `pg_restore --list`; `RUN_RECOVERY_DRILL=true` is accepted only for a database whose name starts `annotated_recovery` and verifies the restored migration ledger. No live provider retention or recovery drill has been run yet.
- 2026-08-04: A read-only Railway CLI audit of the existing staging bucket returned `versioning: Disabled`, `lifecycle: not-configured`, and zero objects. The strict retention flags would fail closed for that configuration; no provider retention or custom-format staging dump is claimed from this workstation, so durable recovery remains external.
- 2026-08-04: The guarded Railway smoke passed on deployment `3e0dddde-4edd-465d-b596-8977668f1789`: the podcast job and annotation reached `ready`, the private media redirect and signed object returned HTTP 200, and the delivered `audio/webm` object was 10,037 bytes before cleanup.
- 2026-08-04: `node --test test/api-acceptance.test.js` now checks that article, video, and podcast annotations preserve their original and canonical source URLs through publish, public detail, and source-type-filtered feed responses. This closes the local API citation contract; deployed browser evidence remains external.
- 2026-08-04: `node --test test/api-acceptance.test.js` now also verifies follow/unfollow profile counts, the following-feed filter, comment persistence, and idempotent like/unlike behavior against a local server. This closes the local social interaction contract; multi-user OAuth and deployed evidence remain external.
- 2026-08-04: Article capture now keeps a real selected passage in the web draft, persists it through reloads, requires it at the API boundary, and prevents the extension from publishing an article with no page selection. `node --test test/validation.test.js test/chrome-extension.test.js` covers the bounds and guard; browser selection evidence remains external.
- 2026-08-04: Failed media jobs now expose a bounded owner-only retry endpoint and public landing-page recovery control. The API acceptance fixture verifies the failed-job transition to `queued` and rejects a duplicate retry after the original job is superseded; media policy tests cover the owner/status guard.
- 2026-08-04: Feed pagination and search now have a bounded API contract. `limit` is clamped to 1–50, malformed cursors and limits fall back safely, cursor pages do not repeat records, and a normalized query returns only matching source/commentary context. `node --test test/api-acceptance.test.js test/source-processing.test.js` covers the end-to-end search and cursor flow; deployed multi-user/browser evidence remains external.
- 2026-08-04: The extension API-origin settings page now has a branded responsive surface, explicit production HTTPS/local-development guidance, keyboard-visible focus, reduced-motion handling, a local-default reset, and visible save/error states. Static Chrome-extension contract tests cover the form, status, and runtime recovery hooks; live packaged-browser inspection remains external.
- 2026-08-04: The following-feed boundary now refuses anonymous requests in authenticated deployments and the web feed prompts before switching filters, preventing an anonymous “Following” view from silently showing “For you.” Feed-auth and UI contract tests cover the guard and recovery path; multi-user browser evidence remains external.
- 2026-08-04: Railway staging deployment `eca36a58-963e-4744-a142-1de7064d36eb` successfully built the current stacked tip and passed `STAGING_ORIGIN=https://annotated-staging.up.railway.app npm run acceptance:staging`. The deployed `/api/feed?following=true` returns `401 {"error":"Sign in is required."}`, while `/api/feed?limit=3` remains public and server-backed; staging currently has X enabled, Google disabled, and an empty feed.
- 2026-08-04: The guarded media smoke ran inside Railway deployment `eca36a58-963e-4744-a142-1de7064d36eb` via service-side SSH, using the private Postgres and bucket network. The direct podcast fixture reached `jobStatus: ready` and `mediaStatus: ready`; the private media route redirected to a signed object that returned HTTP 200 with `audio/webm` and 10,037 bytes, and the script cleaned its temporary records and object.
- 2026-08-04: PR #80 deployment `26e9e2af-b371-4d42-a38b-de291115ed33` passed the expanded guarded media smoke inside Railway service-side SSH. A direct podcast fixture and a direct Sintel MP4 fixture both reached `jobStatus: ready` and `mediaStatus: ready`; private media routes redirected to signed Railway Bucket objects that returned HTTP 200 as `audio/webm` (10,037 bytes) and `video/mp4` (15,897 bytes). The script removed both temporary annotations, jobs, and objects; this proves the deployed audio/video transcode and delivery boundary, not YouTube extraction or browser playback.

## Explicit non-goals from the brief

- Email/password authentication is not required.
- Only Google and X are required identity providers.
- Required source categories are YouTube, news articles, and podcasts; other providers are optional unless the brief changes.
